name: Build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 6.0.x

      - name: Restore
        run: dotnet restore GrannyLegacyClock/GrannyLegacyClock.csproj
        
      - name: Build
        run: dotnet build GrannyLegacyClock/GrannyLegacyClock.csproj --configuration Release
        
      - name: Upload DLL
        uses: actions/upload-artifact@v4
        with:
          name: GrannyLegacyClock
          path: |
            **/bin/Release/net6.0/*.dll
